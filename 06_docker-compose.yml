version: "3.9"

services:
  # Placeholder service definitions.
  # Keep this file minimal until you finalize your actual stack.
  app:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: bash -lc "python --version && sleep infinity"
